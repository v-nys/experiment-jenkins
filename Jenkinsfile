
pipeline {
    agent { label { 'all' } }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
                echo 'It works!'
            }
        }
    }
}
